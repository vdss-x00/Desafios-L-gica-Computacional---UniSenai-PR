programa {
  funcao inicio() {
    inteiro fatorial, numero, resultado=1

    escreva ("Digite um número:")
    leia(numero)

    para(fatorial = 1; fatorial <= numero; fatorial++)
    {
      resultado = resultado * fatorial
    }

    escreva ("O fatorial de ", numero, " é igual a ", resultado)

  }
}
